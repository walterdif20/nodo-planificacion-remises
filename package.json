{
  "name": "nodo-planificacion-bases",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/clean-dist.mjs && vite build && node scripts/prepare-sites.mjs",
    "build:pages": "node scripts/clean-dist.mjs && vite build --mode pages",
    "preview": "vite preview --host 127.0.0.1",
    "preview:pages": "vite preview --mode pages --host 127.0.0.1 --port 4180"
  },
  "dependencies": {
    "leaflet": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-leaflet": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest"
  }
}
